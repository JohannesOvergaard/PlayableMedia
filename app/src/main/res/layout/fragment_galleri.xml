<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GalleriFragment">

   <!-- <ImageView
        android:id="@+id/medal_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/bottle_medal"
        app:layout_constraintBottom_toTopOf="@id/button_galleri_back"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/> -->

    <GridView
        android:id="@+id/medal_grid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnWidth="250px"
        android:gravity="center"
        android:horizontalSpacing="10dp"
        android:numColumns="3"
        android:stretchMode="spacingWidthUniform"
        android:verticalSpacing="10dp"
        android:paddingBottom="50dp"
        android:paddingTop="50dp"
        app:layout_constraintBottom_toTopOf="@id/button_galleri_back"
        />

    <Button
        android:id="@+id/button_galleri_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="252dp"
        android:layout_marginEnd="158dp"
        android:text="@string/back"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/medal_grid" />
</androidx.constraintlayout.widget.ConstraintLayout>